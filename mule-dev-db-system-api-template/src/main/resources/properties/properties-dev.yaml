# API specification
api:
  name: "${project.artifactId}"
  version: "${project.version}"

database:
  host: "cloud-services.demos.mulesoft.com"
  port: "30522"
  username: "admin"
  db: "ps-demo-db"
  
http:
  host: "0.0.0.0"
  port: "8081"
  
https:
  host: "0.0.0.0"
  port: "8081"